import { Box, styled, Button, TextField, Typography } from '@mui/material'

export const Tasks = styled(Box)(({ theme }) => ({
    display: 'flex',
    // alignItems: "center"
}))

export const CustomTypography = styled(Typography)(({ theme }) => ({
    // border: "2px solid red",
    marginBottom: "0.6rem"
}))
